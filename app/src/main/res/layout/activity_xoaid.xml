<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#F5F5F5">

    <!-- ID vân tay -->
    <EditText
        android:id="@+id/id_xoavantay"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Nhập ID muốn xóa"
        android:inputType="number"
        android:padding="12dp"
        android:background="@drawable/round_edittext"
        android:textColor="#000000"
        android:textColorHint="#888888"
        android:textSize="16sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="120dp"/>

    <!-- Điều kiện ID -->
    <TextView
        android:id="@+id/dieu_kienid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="ID hợp lệ từ 1 đến 99"
        android:textSize="14sp"
        android:textColor="#555555"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/id_xoavantay"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Nút xác nhận -->
    <Button
        android:id="@+id/bnt_xoavantayd"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Xác nhận"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:background="@drawable/round_button"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/dieu_kienid"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
